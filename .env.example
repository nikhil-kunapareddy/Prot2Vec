# Copy to .env at the repository root and fill in the keys you need.
# .env is gitignored; never commit real keys, and never put them in a config.
#
# Only required for the `llm` embedder (pip install -e ".[llm]").
# Free tier available at https://aistudio.google.com
GOOGLE_API_KEY=
