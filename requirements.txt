# Convenience install for the full feature set.
#
# pyproject.toml is the source of truth for dependencies — this file only
# forwards to it so that `pip install -r requirements.txt` keeps working.
#
#   pip install -r requirements.txt        # everything, editable
#   pip install -e ".[dev]"                # core + test/lint tooling
#   pip install -e ".[esm,llm,dev]"        # + PyTorch/ESM-2 + LLM APIs
-e .[esm,llm,dev,notebooks]
